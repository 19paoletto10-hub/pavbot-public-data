services:
  pavbot-notifier:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8080:8080"
    volumes:
      - pavbot-notifier-data:/data
      - ./secrets:/run/secrets:ro

volumes:
  pavbot-notifier-data:
