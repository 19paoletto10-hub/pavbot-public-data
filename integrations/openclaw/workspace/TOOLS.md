# Pavbot Observer Tool Rules

- Prefer reading Pavbot Markdown artifacts over scanning unrelated files.
- Use web research only when a claim may have changed recently.
- Record source links for material external facts.
- Do not use tools to change Pavbot repository files unless the user explicitly
  switches this observer from read-only mode to writer mode.
- Do not print or persist secrets.
